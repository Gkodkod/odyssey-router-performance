# Plan 6.3 Summary
- Updated `grafana/provisioning/dashboards/default/router.json` to include two new PromQL-based panels:
  - Throughput by Operation Type (Queries vs Mutations)
  - P99 Latency (Query vs Mutation)
- This completes the segmentation observability.
