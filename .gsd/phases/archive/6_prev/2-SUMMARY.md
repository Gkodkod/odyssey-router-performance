# Plan 6.2 Summary
- Added `updateReview` and `deleteReview` mutations to `reviews` subgraph and schema
- Added `updateProductStock` mutation to `inventory` subgraph and schema
- Updated `router/supergraph.graphql` with new mutations
- Created JSON payloads and appended them to `vegeta/targets-mutations.http`
